# 🛠️ Backend – NestJS API

This is the backend API built with [NestJS](https://nestjs.com/).

---

## 🛠️ Prerequisites

- Node.js = v20.16.0
- npm

---

## 🚀 Setup & Run

1. Clone this repository or go to the backend folder:

   ```bash
   cd app-backend

   ```

2. Install dependencies:

npm install

3. Run the server in development mode:

npm run start:dev
The application will be running at:

http://localhost:3000
